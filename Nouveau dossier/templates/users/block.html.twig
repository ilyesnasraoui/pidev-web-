<form method="post" action="{{ path('users_block', {'idUser': user.idUser}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('block' ~ user.idUser) }}">

    <button class="btn">block</button>
</form>
