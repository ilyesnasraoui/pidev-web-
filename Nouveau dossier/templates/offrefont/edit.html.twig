{% extends 'basefront.html.twig' %}

{% block title %}Edit Offre{% endblock %}

{% block body %}
    <h1>Edit Offre</h1>

    {{ include('offrefont/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('offrefont_index') }}">back to list</a>

    {{ include('offrefont/_delete_form.html.twig') }}
{% endblock %}
