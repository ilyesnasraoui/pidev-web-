<form method="post" action="{{ path('candidaturefont_delete', {'idCandidature': candidature.idCandidature}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ candidature.idCandidature) }}">
    <button class="btn">Delete</button>
</form>
