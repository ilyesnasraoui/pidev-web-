{% extends 'base.html.twig' %}

{% block title %}CategorieEvent index{% endblock %}

{% block body %}
    <h1>CategorieEvent index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>IdCatEvent</th>
                <th>NomCategorieEv</th>
                <th>Description</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for categorie_event in categorie_events %}
            <tr>
                <td>{{ categorie_event.idCatEvent }}</td>
                <td>{{ categorie_event.nomCategorieEv }}</td>
                <td>{{ categorie_event.description }}</td>
                <td>
                    <a href="{{ path('categorie_event_show', {'idCatEvent': categorie_event.idCatEvent}) }}">show</a>
                    <a href="{{ path('categorie_event_edit', {'idCatEvent': categorie_event.idCatEvent}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="4">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('categorie_event_new') }}">Create new</a>
{% endblock %}
