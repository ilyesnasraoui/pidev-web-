{% extends 'basefront.html.twig' %}
{% block body %}
    <div class="hero common-hero">
        <div class="container">
            <div class="row">
                <div class="col-md-5">
                    <div class="hero-ct">
                        <form action="{{ path('resetpwd') }}" method="post">
                       <div class="col-lg-10"><h4>reset code</h4></div>
                        <input type="hidden" name="id" value="{{ user.getIdUser }}">
                        <div class="col-lg-10"><input type="text" name="code"></div>
                        <div class="col-lg-10"><h4>new password</h4></div>
                        <div class="col-lg-10"><input type="password" name="pwd"></div>

                            <div class="col-lg-10"> <input type="submit" value="reset"></div>
                        </form>
                    </div>
                </div>
            </div>

        </div>
    </div>
{% endblock %}