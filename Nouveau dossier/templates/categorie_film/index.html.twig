{% extends 'base.html.twig' %}

{% block title %}CategorieFilm index{% endblock %}

{% block body %}
    <h1>CategorieFilm index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>IdCategorie</th>
                <th>NomCategorie</th>
                <th>Image</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for categorie_film in categorie_films %}
            <tr>
                <td>{{ categorie_film.idCategorie }}</td>
                <td>{{ categorie_film.nomCategorie }}</td>
                <td>{{ categorie_film.image }}</td>
                <td>
                    <a href="{{ path('categorie_film_show', {'idCategorie': categorie_film.idCategorie}) }}">show</a>
                    <a href="{{ path('categorie_film_edit', {'idCategorie': categorie_film.idCategorie}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="4">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('categorie_film_new') }}">Create new</a>
{% endblock %}
