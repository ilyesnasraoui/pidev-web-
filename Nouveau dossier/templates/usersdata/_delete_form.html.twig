<form method="post" action="{{ path('usersdata_delete', {'idData': usersdatum.idData}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ usersdatum.idData) }}">
    <button class="btn">Delete</button>
</form>
